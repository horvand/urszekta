
<% if @hits.size == 0 %>
	<div class="notfound">Ingenting. . .</div>
<!-- size == 1 handled in controller -->
<% else %>
	 <% @hits.each do |hit| %>
	<div class="resultlist">
	<%= link_to hit.cite, "/#{hit.class.table_name}/#{hit.id}" %>
	</div>
	<% end %>
<% end %>
